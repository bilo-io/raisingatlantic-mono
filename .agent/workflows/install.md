---
title: Install
description: Installs the CLI and tests it.
---

# Workflow: /install
Run the following installation command in the terminal:
`bun run cli:install`

If there are any dependency conflicts, suggest a fix before proceeding.

Otherwise, after installing, run `clawesome` to test that the CLI worked, and exit it.
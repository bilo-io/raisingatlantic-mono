project:
  name: raising-atlantic-mono
  
tasks:
  install:
    deps:
      - 'api:install'
      - 'web:install'
      - 'ui:install'
      - 'mobile:install'

  build:
    deps:
      - 'api:build'
      - 'web:build'
      - 'ui:build'
      - 'mobile:build'

  dev:
    deps:
      - 'api:dev'
      - 'web:dev'
      - 'mobile:dev'
    options:
      runInCI: false

  lint:
    deps:
      - 'api:lint'
      - 'web:lint'
      - 'mobile:lint'

  test:
    deps:
      - 'api:test'
      - 'web:test'
      - 'mobile:test'
      - 'tests:test'

  clean:
    deps:
      - 'api:clean'
      - 'web:clean'
      - 'ui:clean'
      - 'mobile:clean'

  db:start:
    deps:
      - 'api:db-start'

  db:stop:
    deps:
      - 'api:db-stop'

  db:migrate:
    deps:
      - 'api:db-migrate'

  db:seed:
    deps:
      - 'api:db-seed'

// TODO: M1.5 — replace this placeholder with parent-specific dashboard cards
// (active child summary, next vaccine due, latest growth entry, unread messages, quick actions).
import React from "react";
import { DashboardHomePlaceholder } from "./DashboardHomePlaceholder";

export function DashboardHomeParent() {
  return <DashboardHomePlaceholder />;
}

// TODO: M3.5 — replace this placeholder with admin-specific dashboard cards
// (platform health, pending verifications, user signups this week, recent admin actions).
import React from "react";
import { DashboardHomePlaceholder } from "./DashboardHomePlaceholder";

export function DashboardHomeAdmin() {
  return <DashboardHomePlaceholder />;
}

# Staging environment variable values.
# Sensitive values are injected via TF_VAR_* environment variables in CI.

# TODO(GCP): replace with the actual staging project ID
project_id = "ra-staging"

region = "africa-south1"

sentry_organization = "raising-atlantic"

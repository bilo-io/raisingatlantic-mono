# Phase M5 — Native, Store & Release: Outstanding Items

> Auto-generated by `/golive-phase` on 2026-07-06 from [MOBILE.md](MOBILE.md). Update manually as tasks are completed.
> Doubles as the last-mile **release checklist** ([§M5.6](MOBILE.md#m56-release-checklist)).

## Completed this session

### M5.1 EAS Build & release channels
- [eas.json](../../src/apps/mobile/eas.json) — `development` / `preview` / `production` build profiles; `appVersionSource: local`.
- EAS Update channels bound to profiles (`development` / `preview` / `production`); `expo-updates` + `expo-dev-client` installed; `runtimeVersion: { policy: fingerprint }` + `updates.url` in [app.json](../../src/apps/mobile/app.json).
- Build-secret mapping: only `EXPO_PUBLIC_USE_API` in `eas.json` `env`; `EXPO_PUBLIC_SENTRY_DSN` (public) and `SENTRY_AUTH_TOKEN` (build-time, unprefixed) documented in [.env.example](../../src/apps/mobile/.env.example), injected via EAS environments — never committed.
- `autoIncrement: true` on the production profile → EAS bumps `ios.buildNumber` / `android.versionCode`.
- Native identity added: `ios.bundleIdentifier` + `android.package` = `com.raisingatlantic.app` (permanent), `buildNumber: "1"`, `versionCode: 1`, `extra.eas.projectId` placeholder (read by [usePushRegistration.ts](../../src/apps/mobile/lib/push/usePushRegistration.ts)).
- Sentry source-map upload ([M4.5 carry-over](MOBILE_PHASE_M4_TODO.md)) is now unblocked: the existing `@sentry/react-native/expo` plugin uploads maps when `SENTRY_AUTH_TOKEN` is present at EAS build time — no new config file needed.

### M5.2 Store assets
- Splash migrated from the legacy `app.json` `splash` key to the `expo-splash-screen` config plugin (kept `#F3F1E9` + `splash-icon.png`); `expo-splash-screen` installed.

### M5.4 Mobile E2E
- Framework decision: **Maestro** — [ADR 0005](../adr/0005-mobile-e2e-framework.md) (closes the open Detox-vs-Maestro choice in DEV.md §12.1 / MOBILE.md §M5.4).
- Per-role smoke flows under [.maestro/](../../src/apps/mobile/.maestro/): `flows/parent-core.yaml`, `flows/clinician-verify.yaml`, `flows/admin-triage.yaml`, plus `common/sign-in.yaml` subflow and `config.yaml`. Mock mode, synthetic identities only.
- Selector instrumentation: 5 `testID`s added where text was ambiguous (`parent-add-child-fab` vs the "Add child" form submit) or absent (form inputs) — [FAB.tsx](../../src/apps/mobile/components/ui/FAB.tsx), [ChildForm.tsx](../../src/apps/mobile/components/children/ChildForm.tsx), [GrowthTab.tsx](../../src/apps/mobile/components/records/GrowthTab.tsx).
- Moon tasks `mobile:e2e` / `mobile:e2e-smoke` ([moon.yaml](../../src/apps/mobile/moon.yaml)); gated `mobile-e2e` CI job on push to `dev` ([ci.yml](../../.github/workflows/ci.yml)), mirroring the `eas-preview` skip-cleanly pattern.

### M5.6 Release checklist
- POPIA **Section 72** cross-border assessment for the shipped SDKs (Sentry, Expo push, FCM/APNs, EAS Update): [MOBILE_POPIA_S72.md](MOBILE_POPIA_S72.md); cross-referenced from [COMPLIANCE.md §4.3](COMPLIANCE.md#43-cross-border-transfers).
- Performance budgets registered: [MOBILE_PERF_BUDGETS.md](MOBILE_PERF_BUDGETS.md) (M5.5 targets + measurement plan + deferred FlashList plan).

## Still outstanding

### M5.2 (DESIGN / store console)
- App icon set (final iOS 1024² + Android adaptive artwork) — placeholders present; **DESIGN**-owned.
- Store screenshots per device class — **DESIGN** + simulator capture.
- iOS privacy nutrition labels — filled in the store console by **PRODUCT/COMPLIANCE**, informed by the DPIA (DEV.md §4.1) + [MOBILE_POPIA_S72.md](MOBILE_POPIA_S72.md).

### M5.3 Store listings (external)
- App Store Connect listing (title, subtitle, description, keywords, support/privacy URLs) — needs an **Apple Developer account**.
- Play Console listing (descriptions, contact, content rating) — needs a **Google Play account**.
- Privacy policy + ToS URLs live and stable — **LEGAL** ([LEGAL.md](LEGAL.md)).
- Health-data declaration on both stores — needs the listings above.

### M5.5 Performance budgets (need a device / native build)
- Cold start < 3s, tab transition < 200ms, 60fps on 500-item lists, < 50MB AAB / < 100MB IPA, per-PR hot-path measurement — targets recorded in [MOBILE_PERF_BUDGETS.md](MOBILE_PERF_BUDGETS.md); none measurable in CI without a device/native build.

### M5.6 (dependency / time-gated / external)
- **Real-auth cutover complete** — blocked on [M4.4](MOBILE_PHASE_M4_TODO.md) / [DEV.md §2.1](DEV.md#21-auth-provider-decision). Hard release blocker; a store-review build must run in mock mode until this lands.
- **Sentry uploads working for the production build** — config done; "working" needs a real production build + live DSN.
- **Privacy policy + ToS URLs live** — LEGAL + live hosting.
- **Crash-free sessions > 99% for 5+ days** on the preview channel — time-gated.
- **E2E smoke green on 3 consecutive runs** — flows committed; needs a device runner + 3 runs.
- **Store review notes with test credentials** — draft below; final submission is external and coupled to the real-auth blocker.

## Blockers / notes
- **Expo account + `eas init`** — required to replace the `extra.eas.projectId` / `updates.url` placeholders, create EAS env vars/secrets (`EXPO_PUBLIC_SENTRY_DSN`, `SENTRY_AUTH_TOKEN`), provision credentials, and run `eas build` / `eas submit`. Everything in `eas.json` is inert until then.
- **Bundle identifier `com.raisingatlantic.app`** — PERMANENT once the first binary is uploaded; cannot change without a new store listing. Sign off before the first `eas submit`.
- **Device/emulator CI runner** — the `mobile-e2e` job is green-by-default and only runs when `vars.RUN_MOBILE_E2E == 'true'`; a runner that boots an Android emulator + installs a debug APK must be provisioned, and the build/boot/install steps wired in `ci.yml`.
- **Native date pickers in Maestro** — the parent-core flow's DOB / growth-date / milestone-date steps are marked `TODO(device)`; fill the platform-specific picker taps on the first real-device run before those forms submit.
- **DEV.md §2.1 auth provider decision** — still the top release blocker (via M4.4).
- **DPA signatures** (Sentry, Expo) + privacy-policy sub-processor disclosure — COMPLIANCE-owned ([§4.3](COMPLIANCE.md#43-cross-border-transfers)).

## Draft store review notes (for M5.6, when submitting)
> The app currently runs on fixture auth (real auth = M4.4, pending). For review, launch the
> app and on the login screen tap **"Continue as Parent"** (or Clinician / Admin) — no
> password required. All data shown is synthetic mock data; no real patient records are
> exercised. A mock-mode review build is required until the real-auth cutover lands.

## Verification checklist (run before merge)
- [ ] `moon run mobile:lint` (tsc `--noEmit`) is clean
- [ ] `moon run mobile:test` (jest-expo) is green
- [ ] `eas.json` and `app.json` are valid JSON; native identity keys present
- [ ] `.maestro/*.yaml` parse as valid YAML
- [ ] `ci.yml` parses; `mobile-e2e` is green-by-default (gated off)
- [ ] Manual (device): `eas init`, then `eas build --profile preview` succeeds
- [ ] Manual (device): Maestro smoke flows run green after date-picker steps are wired

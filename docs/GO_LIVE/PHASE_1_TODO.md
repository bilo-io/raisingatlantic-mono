# Phase 1 — Infrastructure & Hosting Decision: Outstanding Items

> Auto-generated by /golive-phase on 2026-05-27. Update manually as tasks are completed.
> Phase 1 completion after this session: **37 / 83 (44%)**.

## Completed this session

Docs-and-reconciliation slice (no live GCP required). Each box flipped `[x]` in
[DEV.md](DEV.md) is backed by an existing artifact:

- **§1.1 Decide hosting target** — GCP `africa-south1` primary, Vercel marketing-only → [docs/adr/0001-hosting.md](../adr/0001-hosting.md).
- **§1.1 Document decision in ADR** — `docs/adr/0001-hosting.md` exists and is Accepted.
- **§1.1 Inventory Vercel assumptions + confirm portability** — **new** [docs/VERCEL_PORTABILITY.md](../VERCEL_PORTABILITY.md). API already Cloud-Run-bootable; web already `output: 'standalone'`.
- **§1.1 12-month cost on each platform** — [docs/PRICE_ESTIMATE.md](../PRICE_ESTIMATE.md) (GCP 3-yr) + Vercel comparison/rejection in VERCEL_PORTABILITY.md.
- **§1.4 Terraform Cloud vs GCS backend decision** — GCS chosen; `infra/envs/*/backend.tf` already use GCS native locking.
- **§1.4 "What TF does NOT manage" (4 items)** — boundary documented in `infra/README.md` + DEV.md §9.3; image-tag ownership enforced by `lifecycle { ignore_changes = [...image] }` in `infra/modules/cloud-run-service`.
- **§1.5 Neon vs Cloud SQL decision** — migrate to Cloud SQL per ADR 0001.
- **§1.5 Replace `synchronize: true` with migrations for production** — already `synchronize: false` in prod ([app.module.ts:89](../../src/apps/api/src/app.module.ts#L89) `synchronize: !isProd`; [data-source.ts:65](../../src/apps/api/db/data-source.ts#L65)).

## Still outstanding

**Blocked on a live GCP org + billing account** (manual runbook: [TODO_GCP.md](../../TODO_GCP.md)):

- §1.2 — Create GCP org; billing + budget alerts; per-env projects (`ra-dev/staging/prod`); enable APIs; least-privilege IAM SAs; Organization Policies; VPC Service Controls; default-region lock via Org Policy.
- §1.3 — Provision: API → Cloud Run + HTTPS LB + Cloud Armor; Web → Cloud Run standalone; assets → GCS + Cloud CDN; background jobs → Cloud Run Jobs + Cloud Scheduler; email → Pub/Sub; DNS → Cloud DNS + DNSSEC.
- §1.4 — One-time `bootstrap/` apply (tfstate bucket + WIF pool + deployer SAs) and self-import; the 20-item "What Terraform manages" provisioning inventory (code exists as modules + commented env blocks, applied incrementally per TODO_GCP.md Phase 11); IAM-denies-laptop-apply on prod.
- §1.5 — Provision Cloud SQL 15 + HA + backups + 7-day PITR + `pg_dump|pg_restore` rehearsal; private-IP lockdown + Auth Proxy; rotating creds in Secret Manager; nightly logical backups to a 30-day-retention GCS bucket; full DR drill (RTO < 1h / RPO < 15min).

**Recurring process / not code:**

- §1.4 — Annual deployer-SA WIF-binding rotation (calendar reminder); quarterly drift/codify-or-destroy review; Information-Regulator audit answer via `git log infra/envs/prod/iam.tf` (real `iam.tf` lands with the §1.4 provisioning).

**Deferred CI change (not docs, not done here):**

- §1.4 — Make `terraform-apply.yml` reuse the merged-PR plan artifact if reachable; the prod job currently re-plans before apply. Tracked as a follow-up CI task.

**N/A given the decision:**

- §1.5 — "If staying on Neon (confirm region, sign DPA, read replica)" — not applicable; ADR 0001 decides to migrate to Cloud SQL.

## Blockers / notes

- **Central blocker:** no live GCP organization or billing account exists yet. Nothing under §1.2/§1.3 and most of §1.4/§1.5 can be `terraform apply`-ed until that is created via the manual `[CONSOLE]`/`[CLI]` steps in [TODO_GCP.md](../../TODO_GCP.md). The Terraform code (5 modules + commented env resources + plan/apply/drift CI) is already written and waiting.
- This PR deliberately added **no Terraform** — uncommenting env resources without a live project would fail `terraform plan` in CI and contradicts TODO_GCP.md's incremental apply-after-each-block flow.
- VERCEL_PORTABILITY.md lists concrete code follow-ups (env-driven CORS allowlist, fate of `vercel.json` / `apphosting.yaml`) to be handled in §1.3 when Cloud Run services are wired.

# Phase 12 — Pre-Launch Testing: Outstanding Items

> Auto-generated by /golive-phase on 2026-05-20. Last updated 2026-07-05 (feat/golive-dev-phase-12: clinical unit tests, a11y, k6).

## Completed this session

### 2026-07-05 — clinical unit tests · a11y · k6 (feat/golive-dev-phase-12)

- **`pkgs/clinical` wired into the test runner + CI** — was an untested island (only `build`/`clean` scripts, no `moon.yaml`). Added jest + ts-jest config (transpile-only via `isolatedModules`, `module: CommonJS` override), a `test`/`test-cov`/`build` `moon.yaml`, and switched its `@raising-atlantic/types` dep to `file:../types` so `npm install` resolves. `moon run :test` now includes it.
- **Clinical business-logic specs at 100% coverage (37 tests)** — the growth-percentile + EPI-scheduling specs the coverage gap was waiting on:
  - `epi-schedule.spec.ts` — `ageInMonths` (boundaries, invalid/future DOB → 0) and `bucketVaccine` (`due`/`overdue`/`upcoming`/`complete`, at both min and max age-gate boundaries) against the real `epiSchedule` entries, plus data integrity (unique ids, `min ≤ max`, `standardVaccinationSchedule` strips age gates).
  - `growth-charts.spec.ts` — `growthCurve` selector identity + per-dataset invariants (ages 0–60 sequential, strict SD-line ordering, positivity).
  - `milestones.spec.ts` — unique ids, valid categories, expected age bands.
- **Two clinical caveats flagged for clinical review (NOT changed — CLAUDE.md: EPI/growth logic is sign-off-gated):** growth-curve data is synthetic linear approximations, not WHO LMS tables; `growthCurve(_, 'weight-for-height')` silently falls back to weight-for-age.
- **`verifications.service` spec top-up** — asserts the `ra_verifications_pending` queue-depth metric is recorded per type (all/growth/milestone/vaccination) with correct counts + an empty-queue case.
- **k6 load script enhanced** — `tests/k6/staging-load.js` now exercises health + EPI/milestone/growth reference reads (deliberately NOT the leads POST, which is throttled to 3/min/IP); added a `tests:load` moon task. Still needs a run against live staging.
- **cypress-axe accessibility slice** — `auditA11y` helper (gates on critical/serious), specs for public marketing/legal pages + parent/clinician/admin dashboards. Automated slice only; manual keyboard/screen-reader pass still outstanding.

### 2026-05-20 — CI/automation infrastructure

- Cypress smoke suite on every prod deploy — three post-deploy jobs (`cypress-smoke-dev/staging/prod`) added to `.github/workflows/cd-app.yml`
- Postman contract tests run nightly against staging — `.github/workflows/postman-nightly.yml` created (cron 01:00 UTC, soft-fail until staging is live); `test:staging` script wired in `src/test/postman/package.json`
- Postman environment file renamed `test.env.json` → `staging.env.json` so the file name matches its contents (was already labelled "Staging" inside, pointing at `staging-api.raisingatlantic.com`); both `test:test` and `test:staging` scripts now point at it
- Lighthouse score > 90 on landing page — `.github/workflows/lighthouse.yml` runs on every PR touching web code; asserts performance/accessibility/best-practices/seo ≥ 0.9; `.lighthouserc.json` at repo root
- Jest coverage threshold configured — `coverageThreshold` in `src/apps/api/package.json` set as a regression floor (lines/statements 65, functions 50, branches 45) calibrated against PR #7's published 2026-05-24 baseline (66.55 / 53.70 / 46.25); noise exclusions for `.dto.ts`, `.entity.ts`, `.module.ts`, `main.ts`, `db/migrations/`
- CI now gates on coverage — `ci.yml` API job switched from `moon run api:test` to `moon run api:test-cov` (new moon task in `src/apps/api/moon.yaml`); coverage artifact uploaded for 14 days
- k6 load test script created — `tests/k6/staging-load.js`: 100 VUs, 5-min hold, p95 < 500ms, < 1% errors; run with `k6 run --env BASE_URL=<url> tests/k6/staging-load.js`

## Still outstanding

- [/] Unit test coverage > 70% — the **EPI-scheduling + growth-percentile business logic is now covered at 100%** in `pkgs/clinical` (see this session's work above), and verification metrics are covered. What remains is the **API-project global** number reaching 70% lines/statements, which is **blocked**: the API unit suite is currently red on `dev` (see the JwtAuthGuard regression under Blockers), so global coverage cannot be measured or ratcheted until that is fixed. Once green, add service/controller specs for the largest remaining gaps (children/appointments/users/master-data + thin controllers) and raise the floor toward 70.
- [ ] Mobile E2E (Detox or Maestro) on critical flows: signup, add child, log growth — requires iOS/Android simulator environment and native toolchain setup
- [ ] Internal alpha, the team + 2-3 friendly testers for 2 weeks
- [ ] Clinical accuracy review by a registered paediatrician (EPI schedule, milestone wording, growth chart correctness)
- [ ] Accessibility audit (axe-core + manual keyboard nav + screen reader on dashboard) — Lighthouse CI covers automated a11y on public pages; **cypress-axe now audits public pages + parent/clinician/admin dashboards** (`cypress/e2e/a11y/`, gating on critical/serious); the **manual** keyboard-nav + screen-reader pass on the dashboard is still needed
- [ ] Multi-language QA (i18n is wired, verify Afrikaans + Zulu translations are actually correct)
- [ ] Mobile device matrix, at minimum: iPhone 13, Pixel 7, mid-range Android (Samsung A-series)
- [ ] API load test (k6 or Artillery) against staging, 100 concurrent users, sustained 5min — script is ready in `tests/k6/staging-load.js`; needs to be run against a live staging environment
- [ ] DB slow-query log review, every query > 100ms gets an index review — requires live Cloud SQL with real traffic
- [ ] Cold-start benchmarks on Cloud Run (set min-instances if needed) — requires live Cloud Run in `africa-south1`
- [ ] Recruit 3-5 paediatric practices for closed beta (1-month free + feedback calls)
- [ ] Onboard their parents through a waitlist (`/v1/leads` already captures this)
- [ ] Weekly feedback sync via Slack `#clinician-feedback`
- [ ] Define exit criteria for closed → open beta (zero P0 bugs in 14 days, NPS > 30)

## Blockers / notes

- **🔴 API unit suite is RED on `dev` (pre-existing regression, blocks the coverage gate).** The phase-2 auth merge (PR #66) changed `JwtAuthGuard` to inject `JwtService` + `ConfigService`. Every controller spec that builds a `TestingModule` with `@UseGuards(JwtAuthGuard, …)` but does not override the guard now fails DI: `Nest can't resolve dependencies of the JwtAuthGuard (?, ConfigService)` (confirmed on `blog.controller`, `tenants.controller`; ~10 controller specs affected). `auth.service.spec` is also failing and pathologically slow (~69s). Because CI runs `moon run api:test-cov`, the API job is red until fixed. **Fix:** add `.overrideGuard(JwtAuthGuard).useValue({ canActivate: () => true })` (and `RolesGuard`, `JwtVerifiedGuard`) to the affected controller specs, and diagnose the slow `auth.service.spec`. This should be its own focused PR; the API-global-70% ratchet is gated on it. (The `pkgs/clinical` suite added here is a separate jest project and is unaffected — it is green at 100%.)
- **Cypress smoke (dev/staging)** jobs are `continue-on-error: true` until the respective Cloud Run environments are stable and test users are seeded. Tighten to hard-fail once environments are fully bootstrapped.
- **Postman nightly** is `continue-on-error: true` until `ra-api-staging` is reachable. Same trigger: remove once infra Phase 1 is bootstrapped.
- **Lighthouse** accessibility assertion is a hard `error`; performance/best-practices/seo are `warn`. This is intentional — don't block PRs on performance before the app is production-polished, but accessibility failures are non-negotiable.
- **Unit test coverage**: the regression floor (lines/statements 65, functions 50, branches 45) is enforced in CI via `moon run api:test-cov`. Floor was set to PR #7's published 2026-05-24 baseline minus ~1–2pt so it blocks regressions without breaking CI on day one. Ratchet the floor up by ~2pt per round of new specs until the 70% target is hit.
- **k6 staging URL**: update `tests/k6/staging-load.js` default `BASE_URL` once the staging Cloud Run URL is confirmed.

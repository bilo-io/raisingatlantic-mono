# Phase 13 — Launch & Marketing: Outstanding Items

> Auto-generated by /golive-phase on 2026-06-30 from docs/GO_LIVE/DEV.md. Update manually as tasks are completed.

## Completed this session

DEV-owned technical SEO + analytics, shipped for **both** Next.js apps (`src/apps/web` →
raisingatlantic.com, `src/apps/pedicheck` → pedicheck.co.za):

- **Open Graph + Twitter card metadata on every public route.** Set `metadataBase` on the web root
  layout (was unset — OG image URLs were relative and broke on social scrapers; now absolute), added
  missing `description` to `/about` and `/contact`, and `noindex` to the internal `/design-system`
  group. PediCheck root metadata already carried decoupled OG/Twitter copy.
- **Sitemap + robots.txt.** New `app/sitemap.ts` + `app/robots.ts` in both apps. Web sitemap lists all
  marketing routes + 9 legal slugs and best-effort appends blog posts from `/v1/blog` (try/catch, ISR
  `revalidate=3600` so an API outage can't fail the build). Robots disallow dashboard/api/internal and
  point at the sitemap.
- **Google Search Console verification.** Wired as an env-gated `verification.google` `<meta>` in both
  root layouts (`NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`). Code complete — see blocker for the token.
- **Analytics: Plausible (cookieless, POPIA-friendly — no consent banner needed).** Env-gated
  `<PlausibleAnalytics>` component in both apps; emits no script until `NEXT_PUBLIC_PLAUSIBLE_DOMAIN`
  is set (verified: 0 occurrences in built HTML with the env unset).
- **JSON-LD structured data.** `Organization` + `WebSite` schema in both apps' root layouts.
- Documented all new `NEXT_PUBLIC_*` SEO vars in both `.env.example` files.
- Verified: `next build` passes for both apps; PediCheck's build runs TypeScript clean; generated
  `robots.txt` + `sitemap.xml` inspected; OG images render as absolute URLs.

## Still outstanding

DEV-scoped: **none** — the three DEV-owned §13 items are complete. Remaining §13 lines are owned by
other roles or blocked:

- [ ] Final landing page copy and SEO polish — **MARKETING** (copy); SEO metadata mechanics are done.
- [ ] Launch announcement channels (LinkedIn, paediatric associations, parenting groups) — **MARKETING**.
- [ ] Press kit (logo, screenshots, founder bio, one-pager) — **DESIGN**.
- [ ] Pricing-page CTA → Stripe Checkout (live) — **blocked on Phase 3 (Stripe)**; PRODUCT owns pricing.
- [ ] Demo booking flow via Calendly for B2B — **PRODUCT**.

## Blockers / notes

- **Google Search Console token** — engineering is done, but the actual verification token must be
  generated in Search Console and pasted into `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` per environment
  (or verify via DNS instead). External action.
- **Plausible account + domain** — the integration is inert until a Plausible account exists and
  `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` (+ optional self-hosted `NEXT_PUBLIC_PLAUSIBLE_SRC`) is set in the
  deploy environment. Decision recorded: Plausible over PostHog to avoid POPIA consent-banner overhead.
- **`NEXT_PUBLIC_SITE_URL`** defaults to the production origin per app; override per environment so
  staging/preview sitemaps don't emit production URLs.
- PostHog was explicitly **not** chosen this round (cookies/local storage → consent management).

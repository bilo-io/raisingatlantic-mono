# Phase 5 — Security: Outstanding Items

> Auto-generated by `/golive-phase` on 2026-05-28. Update manually as tasks
> are completed.

## Completed this session

All of §5.1 was landed in `feat/golive-phase-5`:

- OWASP Top 10 audit document (`docs/security/OWASP_TOP_10_AUDIT.md`)
- Helmet middleware in [src/apps/api/src/main.ts](../../src/apps/api/src/main.ts) with prod-gated HSTS
- Strict CORS allowlist confirmed (already in place; verified)
- Global `UserAwareThrottlerGuard` + named tiers in
  [src/apps/api/src/app.module.ts](../../src/apps/api/src/app.module.ts);
  leads form pinned to 3/min
- DTO gap-fixes: `CreatePracticeDto` lat/lng bounds, `CreateReportDto.content`
  via new `MaxObjectDepth` validator + unit tests
- `dangerouslySetInnerHTML` removed from the legal page; shared
  `MarkdownContent` primitive routes blog + legal through `react-markdown`
- Dependabot for 5 npm workspaces + github-actions + terraform + docker
- `npm audit` matrix in `ci.yml` covering api/web/mobile/ui/types
- gitleaks workflow + `.gitleaks.toml` with placeholder allowlist
- `lefthook` pre-commit running gitleaks; `pre-push` running lint
- ADR 0003 records the baseline

## Still outstanding

### §5.2 Infrastructure Security — blocked on Phase 1 GCP foundation

- [ ] WAF (Cloud Armor) in front of public endpoints with OWASP rules + geo-rate-limiting
- [ ] DDoS protection (included with Cloud Armor)
- [ ] TLS 1.2+ enforced; HSTS preload at the load balancer
- [ ] All inter-service traffic over private VPC
- [ ] Cloud Armor bot management for `/v1/leads` and `/v1/auth/*`
- [ ] Database accessible only via private IP + Cloud SQL Auth Proxy
- [ ] No service-account JSON keys on disk — Workload Identity Federation

**Blocker:** the Phase 1 GCP projects, VPC, and Cloud Run services do not
yet exist. Cannot author or `terraform plan` these resources until §1.2 /
§1.4 are complete.

### §5.3 Data Security — partial, mostly blocked

- [ ] Database encryption at rest (confirm + document) — **doc-only;
  trivial once Phase 1 lands. Default-on for both Neon and Cloud SQL.**
- [ ] Application-level encryption for HPCSA / SANC / child medical
  conditions via GCP KMS — **Tier-3 follow-up; requires KMS keys from
  Phase 1 + a migration strategy. ADR-worthy on its own.**
- [ ] PII redaction in logs — **blocked on Phase 7.1 (Pino + Cloud
  Logging). Premature until structured logging exists.**
- [ ] Pseudonymisation in non-prod — **verify-only; seed scripts already
  use synthetic data. Add an explicit test asserting no email/HPCSA/DOB
  shape leaks into seed output.**
- [ ] Backup encryption with CMEK — **blocked on Phase 1 GCS buckets.**

### §5.4 Operational Security — non-DEV / procurement

- [ ] Mandatory 2FA on GitHub, GCP, Stripe, Neon, Workspace, registrar —
  **non-DEV; OPS task to enforce org-wide.**
- [ ] Hardware security keys (YubiKey) for `SUPER_ADMIN` — **procurement.**
- [ ] Quarterly access review — **OPS cadence task.**
- [ ] Annual third-party penetration test — **OPS to procure vendor.**
- [ ] Bug bounty program (private on HackerOne / Bugcrowd) — **defer until
  post-launch stability.**

## Blockers / notes

- **GitHub Advanced Security:** repo-native secret scanning + push
  protection is a paid feature on private repos. gitleaks-action is the
  substitute we ship; revisit if we move to a plan that includes GHAS.
- **Bun vs npm in CI:** the CI pipeline currently uses `npm` (per-app
  `package-lock.json`). The DEV.md checklist mentions `bun audit`; we ship
  `npm audit` to match the actual pipeline and tracker the switch as a
  separate concern (likely paired with a full toolchain migration).
- **Helmet header overlap:** when Phase 1's Cloud Armor / GLB lands, audit
  for duplicate HSTS / X-Frame-Options between Helmet and the LB. Decide
  one source of truth per header.

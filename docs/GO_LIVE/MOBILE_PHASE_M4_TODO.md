# Phase M4 — Polish & Platform UX: Outstanding Items

> Auto-generated by `/golive-phase` on 2026-06-03 from [MOBILE.md](MOBILE.md). Update manually as tasks are completed.

## Completed this session

### M4.1 Push notifications
- `expo-notifications` + `expo-device` installed and wired into [AuthContext.tsx](../../src/apps/mobile/auth/AuthContext.tsx)
- Token registration / deregistration via [usePushRegistration.ts](../../src/apps/mobile/lib/push/usePushRegistration.ts) → [push-tokens.adapter.ts](../../src/apps/mobile/lib/api/adapters/push-tokens.adapter.ts) (mock + real; mock by default)
- Role-aware topic list in [topics.ts](../../src/apps/mobile/lib/push/topics.ts)
- Quiet-hours UI added to parent profile ([ProfileScreenParent.tsx](../../src/apps/mobile/components/profile/ProfileScreenParent.tsx)); persisted to AsyncStorage under `@ra/quiet-hours`
- Foreground notification handler in [handler.ts](../../src/apps/mobile/lib/push/handler.ts) with PII-shape warning via [scrub.ts](../../src/apps/mobile/lib/push/scrub.ts)
- `expo-notifications` plugin added to [app.json](../../src/apps/mobile/app.json); `NSUserNotificationsUsageDescription` set

### M4.2 Offline & retry
- Mutation `retry: 3` + exponential backoff in [query-client.ts](../../src/apps/mobile/lib/api/query-client.ts); skips on 400/401/403/404/409/422
- 401 → sign-out via [sign-out-bridge.ts](../../src/apps/mobile/lib/api/sign-out-bridge.ts); interceptor in [client.ts](../../src/apps/mobile/lib/api/client.ts)
- [OfflineBanner.tsx](../../src/apps/mobile/components/OfflineBanner.tsx) mounted in [_layout.tsx](../../src/apps/mobile/app/_layout.tsx)
- Existing optimistic-delete pattern in [createResourceHooks.ts](../../src/apps/mobile/lib/api/createResourceHooks.ts) covers the "rollback on persistent failure" requirement

### M4.3 Deep linking
- `associatedDomains` + Android `intentFilters` added to [app.json](../../src/apps/mobile/app.json)
- [pending.ts](../../src/apps/mobile/lib/linking/pending.ts) + [useDeepLinkGate.ts](../../src/apps/mobile/lib/linking/useDeepLinkGate.ts) handle cold/warm start and auth-gate redirect
- [login.tsx](../../src/apps/mobile/app/\(auth\)/login.tsx) consumes the pending link after sign-in

### M4.4 Real auth scaffolding (lines remain `[ ]` by design)
- [StorageDriver](../../src/apps/mobile/auth/storage.ts) interface + `AsyncStorageDriver` impl; `setStorageDriver()` ready for `SecureStoreDriver`
- [secure-token.ts](../../src/apps/mobile/auth/secure-token.ts) — `expo-secure-store` wrapper for the real ID token (unused at runtime, ready for cutover)
- [provider.ts](../../src/apps/mobile/auth/provider.ts) — `AuthProvider` interface + `FixtureAuthProvider` (active) + `FirebaseAuthProvider` stub
- [AuthContext.tsx](../../src/apps/mobile/auth/AuthContext.tsx) delegates to the active provider; behavior unchanged in fixture mode

### M4.5 Error boundaries & Sentry
- [ErrorBoundary.tsx](../../src/apps/mobile/components/ErrorBoundary.tsx) — root + per-role boundaries wrapping the three `(app)/(role)/_layout.tsx` files
- [init.ts](../../src/apps/mobile/lib/sentry/init.ts) reads `EXPO_PUBLIC_SENTRY_DSN`; init no-ops if empty (logs `"DSN not set, init skipped"`)
- [scrub.ts](../../src/apps/mobile/lib/sentry/scrub.ts) redacts emails, SA 13-digit IDs, HPCSA / SANC numbers, phone numbers, and values under sensitive keys (`name`, `email`, `dob`, `message_body`, …)
- Unit tests in [__tests__/scrub.test.ts](../../src/apps/mobile/lib/sentry/__tests__/scrub.test.ts)

### M4.6 Accessibility
- [Button.tsx](../../src/apps/mobile/components/ui/Button.tsx): `minHeight` 44/48, `hitSlop`, `accessibilityLabel`, `accessibilityState`
- [IconButton.tsx](../../src/apps/mobile/components/ui/IconButton.tsx): dynamic `hitSlop` expands to the 44/48 minimum
- Verified no `allowFontScaling={false}` anywhere in `src/apps/mobile/`
- [MOBILE_A11Y_AUDIT.md](MOBILE_A11Y_AUDIT.md) — per-role per-screen checklist with WCAG ratio table for light + dark themes

## Completed 2026-07-16 — M4.4 full real-auth cutover

The §2.1 decision landed as **build-your-own NestJS JWT**, so the cutover targets `/v1/auth/*`, not Firebase:

- `ApiAuthProvider` in [provider.ts](../../src/apps/mobile/auth/provider.ts) — active when `EXPO_PUBLIC_USE_API=true`; `signInWithPassword(email, password)` against `POST /v1/auth/login` (Bearer flow; token also returned in the body for non-cookie clients), plus `completeMfaChallenge` / `setupMfa` / `enableMfa`. `FixtureAuthProvider` unchanged for mock mode.
- **Email verification + password reset** — new API endpoints (`/v1/auth/verify-email[/request]`, `/v1/auth/password-reset[/request]`, hashed single-use tokens) + mobile screens [verify-email.tsx](../../src/apps/mobile/app/(auth)/verify-email.tsx), [forgot-password.tsx](../../src/apps/mobile/app/(auth)/forgot-password.tsx), [reset-password.tsx](../../src/apps/mobile/app/(auth)/reset-password.tsx) (deep-linked `?token=` supported). Login is blocked with 403 `EMAIL_NOT_VERIFIED` until verified.
- **MFA enforced** for clinician/admin/super_admin — TOTP; login returns a 5-min scoped token; [mfa.tsx](../../src/apps/mobile/app/(auth)/mfa.tsx) handles both first-time enrolment (secret + otpauth URL) and the recurring challenge. Parents can opt in.
- **SecureStoreDriver** in [storage.ts](../../src/apps/mobile/auth/storage.ts) — user + JWT live in the device keychain in API mode (keys sanitised for SecureStore); AsyncStorage remains fixture-only.
- **Fixture-JWT injection removed from AuthContext** — publishing moved inside `FixtureAuthProvider` behind `__DEV__` with an inline require, so API-mode/production code paths never touch `fixture-jwt.ts`. The `X-User-Id`/`X-User-Role` shim headers are now fixture-mode-only too.
- 401 interceptor no longer signs the user out for auth-flow endpoints (bad password ≠ dead session).

## Still outstanding

### M4.5 Sentry — source-map upload
- Deferred to [§M5.1](MOBILE.md#m51-eas-build--release-channels). Needs `eas.json` and a Sentry org / DSN.

## Blockers / notes

- **SMTP env unset** — verification/reset emails dispatch through the Phase-8 seam and silently no-op until `SMTP_HOST/USER/PASS` are configured (DEV.md §8.1). The flows and tokens work; delivery needs the provider.
- **Web UX gap** — web `/login` doesn't yet handle `EMAIL_NOT_VERIFIED` / MFA challenge responses (tracked in PHASE_2_TODO.md).
- **Session lifetime on mobile** — access token is 15 min with no refresh rotation (deferred §2.2 item): expiry surfaces as a 401 → sign-out → re-login.
- **Sentry org / DSN** — needs Sentry account + project provisioning; until `EXPO_PUBLIC_SENTRY_DSN` is set, `initSentry()` is a no-op.
- **Backend `POST /v1/users/me/push-tokens`** — pushTokens adapter ships with a `useApi()` switch; until DEV.md §8.3 delivers the endpoint, the mock branch persists tokens locally.
- **EAS account + `eas.json`** — required for §M5.1 and for the M4.5 source-map upload.
- **A11y follow-ups** — TalkBack passes pending Android device time; chart label and vaccine-chip state announcements recorded in [MOBILE_A11Y_AUDIT.md](MOBILE_A11Y_AUDIT.md).

## Verification checklist (run before merge)

- [ ] `bunx tsc --noEmit` from `src/apps/mobile/` is clean
- [ ] `bun test src/apps/mobile/lib/sentry/__tests__/scrub.test.ts` is green
- [ ] Manual: offline banner appears when Wi-Fi is dropped
- [ ] Manual: cold-start `raisingatlantic://(app)/(parent)/children` routes to children after sign-in
- [ ] Manual: `EXPO_PUBLIC_SENTRY_DSN=""` → "DSN not set, init skipped" appears in the dev log

# Phase 9 — CI/CD & Release Engineering: Outstanding Items

> Auto-generated by /golive-phase on 2026-05-20. Update manually as tasks are completed.

## Completed this session

- [x] `ci.yml` — PR gate: Lint, API Tests, Web Build, Typecheck, Security Audit, Mobile Typecheck, EAS Preview (conditional)
- [x] `cd-app.yml` — CD pipeline: build + trivy scan + SBOM (syft) + cosign sign + push to Artifact Registry; deploy to dev/staging/prod with canary rollout and automated rollback
- [x] `src/apps/api/Dockerfile` — multi-stage NestJS production image (non-root, node:20-alpine)
- [x] `src/apps/web/Dockerfile` — multi-stage Next.js standalone image (non-root, node:20-alpine)
- [x] `src/apps/web/next.config.ts` — added `output: 'standalone'` required for standalone Docker build
- [x] `infra/bootstrap/main.tf` — added `app-deployer-dev/staging/prod` service accounts with WIF bindings; scoped to `roles/run.developer` + `roles/artifactregistry.writer` (not org-wide editor)
- [x] `infra/bootstrap/outputs.tf` — added outputs for new SA emails (set as `APP_SERVICE_ACCOUNT_*` GitHub Secrets)
- [x] `infra/envs/dev/main.tf` — uncommented `github_branch_protection.dev` (required checks: Lint, API Tests, Web Build)
- [x] `infra/envs/prod/main.tf` — uncommented `github_branch_protection.main`; added `github_repository_environment` resources for `production`, `staging`, and `dev` environments
- [x] `infra/envs/prod/variables.tf` — added `github_owner` variable for production environment reviewer

## Still outstanding

### Blocked on Phase 1 (GCP bootstrap not yet run)

The CI/CD code is written and ready. The following will not execute successfully until Phase 1 GCP resources exist:

- Cloud Run services (`ra-api-dev`, `ra-web-dev`, `ra-api-staging`, `ra-web-staging`, `ra-api-prod`, `ra-web-prod`) must be created by `infra/envs/*/main.tf`
- Artifact Registry repositories must exist at `africa-south1-docker.pkg.dev/ra-{dev,staging,prod}/ra-images/`
- Migration Cloud Run Jobs (`ra-migrate-dev`, `ra-migrate-staging`, `ra-migrate-prod`) must be created in each project (cd-app.yml skips them gracefully with a warning until they exist)
- IAM grants for `app-deployer-*` SAs must be uncommented in `infra/bootstrap/main.tf` once GCP project IDs are known

### Needs GitHub setup (after `terraform apply` on prod env)

- Set GitHub Secrets: `APP_WORKLOAD_IDENTITY_PROVIDER`, `APP_SERVICE_ACCOUNT_DEV`, `APP_SERVICE_ACCOUNT_STAGING`, `APP_SERVICE_ACCOUNT_PROD`, `SLACK_WEBHOOK_URL`
- GitHub Environments (`production`, `staging`, `dev`) are managed by Terraform — apply `infra/envs/prod/` to create them
- Add yourself as required reviewer on the `production` environment in GitHub Settings → Environments (or let Terraform manage it via `github_owner` variable)

### Needs credentials

- `EXPO_TOKEN` — optional GitHub Secret to enable EAS Preview builds in CI; the job is `continue-on-error: true` until this is set
- `INFRACOST_API_KEY` — referenced in `terraform-plan.yml`; needed for cost-diff comments on infra PRs

### Deferred to later phases

- **Postman contract tests in CI** — require a live API endpoint; add as a post-deploy smoke test once staging is live (Phase 12)
- **Cypress E2E in CI on `main` PRs** — `github_branch_protection.main` lists it as a required check, but the job itself must be added to `ci.yml` once the web staging URL is known (Phase 12)
- **EAS production builds + App Store submission** — Phase 10
- **Feature flags provider choice** (GrowthBook / Flagsmith / PostHog) — provider not yet selected; Terraform placeholder ready

## Blockers / notes

- The `cd-app.yml` canary rollout assumes `gcloud` CLI is available and the app-deployer SA has been granted `roles/run.developer`. Until Phase 1 bootstrap runs, the deploy jobs will fail with "service not found" — this is expected.
- `cosign sign` uses keyless signing (Sigstore Fulcio) by default; if a GCP KMS key is preferred, update the sign step to `cosign sign --key gcpkms://...`.
- The `github_repository_environment_deployment_policy` resource requires `hashicorp/github` provider ≥ 5.25. Verify the pinned version in `infra/envs/prod/versions.tf` supports it.

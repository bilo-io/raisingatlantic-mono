# Phase 4 — POPIA Compliance: Outstanding Items

> Auto-generated by `/golive-phase` on 2026-06-30 from `docs/GO_LIVE/DEV.md`. Update manually as tasks are completed.

Phase 4 is **stakeholder-led** (`COMPLIANCE 50%` · `LEGAL 30%` · `DEV 20%`). The only DEV-executable slice is §4.2 (Consent & Data Subject Rights). This session delivered the read-only, schema-free portion of §4.2.

## Completed this session

- [x] **§4.2 Data portability — machine-readable export.** New `privacy` module
  (`src/apps/api/src/privacy/`): `PrivacyService.exportUserData()` aggregates the
  data subject's profile + each child (growth, milestones, vaccinations, allergies,
  conditions) into one structured JSON document. Exposed at `GET /v1/privacy/export/:userId`.
  Unit-tested (`privacy.service.spec.ts`, 3 cases). No schema change.

## Still outstanding (§4.2 — DEV)

- [/] **DSAR endpoint** — the JSON export above satisfies the machine-readable half;
  **PDF rendering** and **dashboard wiring** (web + the mobile "data-export trigger"
  stub) remain. PDF needs a render dependency; left for a focused follow-up.
- [ ] **Right to erasure** — needs a soft-delete column on `User` (the entity has
  no `status`/`deletedAt` today; `users.service.remove()` is a hard delete). That is
  a **schema migration** + 30-day-grace scheduled hard-delete job, and per CLAUDE.md
  must be DB-backed-tested (real Postgres) — deferred to an iteration that can run
  migrations + integration tests, not mocked.
- [ ] **Granular consent capture** + **consent versioning** + **parental consent record** —
  new `Consent` entity + migration; consent copy/legal-basis wording needs `LEGAL`/`COMPLIANCE`.
- [ ] **Right to rectification** — largely covered by existing `PATCH` update endpoints;
  proper self-scoping ("users correct *their own* data") depends on Phase 2 auth (`req.user`).

## Blockers / notes

- **Self-scoping (security):** `GET /v1/privacy/export/:userId` currently takes the id as a
  param under the app-wide **placeholder** `JwtAuthGuard`. Phase 2 auth MUST enforce
  `userId === req.user.id` (or ADMIN) before this goes live — flagged in a controller comment.
  A subject must never export another subject's PII.
- **Out of DEV scope (COMPLIANCE/LEGAL):** §4.1 Information Officer registration / PIIA / RoPA,
  §4.3 cross-border DPAs (POPIA s72), §4.4 breach-notification runbook + tabletop.
- **No PII in logs:** the export returns PII by design (it is the subject's own data); it is
  never logged.

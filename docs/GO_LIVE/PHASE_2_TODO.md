# Phase 2 — Authentication & Identity: Outstanding Items

> Auto-generated by /golive-phase on 2026-07-01 from docs/GO_LIVE/DEV.md. Update manually as tasks are completed.

## Completed this session

Real authentication (email/password + Google SSO) on the web `/login`, backed by a from-scratch
NestJS JWT auth module. Session is an **httpOnly cookie** (XSS-safe). The `/login/test` fixture bypass
still works with no GCP.

- **§2.1 Auth provider decided:** build-your-own NestJS JWT (Firebase deferred — needs GCP).
- **API auth module** (`src/apps/api/src/auth/`): `POST /v1/auth/register|login|google|logout`,
  `GET /v1/auth/me`. bcrypt (`bcryptjs`, cost 12) hashing; `@nestjs/jwt` signing; token in an httpOnly
  cookie (`ra_access_token`, `sameSite=lax`, `secure` in prod).
- **Google SSO** verified server-side with the already-installed `google-auth-library` (no new dep);
  find-or-create user. Endpoint returns 503 + web hides the button until `GOOGLE_CLIENT_ID` /
  `NEXT_PUBLIC_GOOGLE_CLIENT_ID` is set (env-gated — no GCP infra required).
- **User columns** (`passwordHash` [`select:false`], `googleId`, `authProvider`, `emailVerified`) +
  hand-written migration `AddAuthFieldsToUser`.
- **Guards:** real `JwtVerifiedGuard` (strict, protects `/auth/me`); `JwtAuthGuard` upgraded from a
  `return true` stub to best-effort verification (attaches `req.user` when a token is present, never
  blocks) so the 8 controllers already using it are non-breaking. `JwtModule` is global.
- **§2.2 rate-limit** on login/google (`@Throttle` 5/60s); **audit log** of login/logout/register to
  `SystemLog` (no PII).
- **Web:** `/login` real email/password form + env-gated Google button; api-client sends the cookie
  (`withCredentials`), retires the mock `X-User-Id`/`X-User-Role` header bridge, redirects to `/login`
  on a live-session 401; `useCurrentUser` hydrates from `GET /v1/auth/me`; signup wizards call the real
  register endpoint.
- **Tests:** API auth specs pass against real Postgres (register/login/getMe/google-unavailable +
  optional/strict guard specs). Web + API typecheck clean; web build green.

## Still outstanding (deferred within Phase 2)

- [ ] Email verification required before first login (§2.2) — needs a live email provider (Phase 8).
- [ ] Password reset flow (§2.2) — needs email provider.
- [ ] MFA (mandatory clinician/admin, optional parent) (§2.2).
- [ ] Account lockout after N failed attempts (§2.2).
- [ ] Refresh-token rotation + logout-everywhere (§2.2) — only a short-lived access token today.
- [ ] Audit password-change / role-change events (§2.2).
- [ ] Clinician verification: HPCSA/SANC regex, cert upload to GCS, annual re-verify (§2.3).
- [ ] **Strict per-route enforcement:** flip the 8 controllers from best-effort `JwtAuthGuard` to
      strict, add `@Public()` for genuinely public routes (e.g. `GET /practices/public`), audit each.

## Blockers / notes

- **Google OAuth client id** (external): create a free client at console.cloud.google.com → APIs &
  Services → Credentials, then set `GOOGLE_CLIENT_ID` (API) + `NEXT_PUBLIC_GOOGLE_CLIENT_ID` (web).
  Not GCP infra — just an OAuth credential. Google login is inert until set.
- **`/login/test` bypass** is gated by `NEXT_PUBLIC_ENABLE_TEST_LOGIN` (default off in prod). It seeds a
  mock session for local dev without GCP; with `NEXT_PUBLIC_USE_API=true` and no real cookie, data
  endpoints stay reachable because `JwtAuthGuard` is best-effort this round.
- **Mobile auth cutover** stays on the MOBILE roadmap (§M4.4) — out of scope here.
- **DB migration** runs in prod (`synchronize:false`); dev/test rely on `synchronize:true`.

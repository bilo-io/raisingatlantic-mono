# Phase M3 — Admin Flow: Outstanding Items

> Auto-generated by /golive-phase on 2026-06-03 from docs/GO_LIVE/MOBILE.md. Update manually as tasks are completed.

## Completed this session

- §M3.1 Users — `users/index.tsx` with search + role filter (parent / clinician / admin / super_admin), backed by a new adapter-routed `useUsers()` hook so mock mode actually returns fixture data
- §M3.1 — Tap-through user detail at `users/[id].tsx` (read-only, "RBAC edits available on web" hint)
- §M3.2 Verifications — `verifications.tsx` wraps a new shared `<VerificationsQueue scope="admin" />` (also reusable by M2.2 clinician later)
- §M3.2 — Filter chips: record type (Growth / Milestone / Vaccination) and HPCSA / SANC clinician registration board
- §M3.3 System — `system.tsx` renders platform health (API / DB / queue badges + last-checked), feature flags (read-only with rollout %), and tenant summaries
- §M3.4 Activity — `activity.tsx` reads from new `useActivity()` hook and filters client-side to high-signal types (auth_failure, verification_state_change, role_change, tenant_created, password_reset). No PII in row copy.
- §M3.5 Dashboard — `DashboardHomeAdmin` variant with platform health card (tap → system), pending verifications count (tap → verifications), 7-day signups, recent admin actions
- §M3.6 Profile — `ProfileScreenAdmin` adds admin-scope badge (`Tenant admin` vs `Super admin`, derived from `useMe().role`) and a "my recent actions" card filtered to `actorId === me.id`

### Supporting work

- `pkgs/types/src/system.ts` — new shared types: `SystemHealth`, `FeatureFlag`, `TenantSummary`, `SystemLog`, `SystemLogLevel`, `SystemComponentStatus` (zod schemas mirror the existing types package convention)
- `lib/api/fixtures/users.ts` — expanded from 3 → 9 synthetic users across roles and recency to make filters visibly work; no real PII
- `lib/api/fixtures/system.ts` + `fixtures/activity.ts` — new fixtures
- `lib/api/adapters/system.adapter.ts` + `adapters/activity.adapter.ts` — both honour `useApi()` toggle
- `lib/api/hooks/users.ts` — added `useUsers(params)` and `useUserById(id)` wrappers (the `createResourceHooks`-generated `useUsersList` bypasses the mock toggle, so admin screens needed adapter-routed alternatives)
- `lib/api/hooks/system.ts` + `hooks/activity.ts` — new
- `components/verifications/VerificationsQueue.tsx` — shared component, ready for M2.2 to reuse with `scope="clinician"`

## Still outstanding

None — phase complete.

## Blockers / notes

- **Tenant filtering on Users screen** — the roadmap says "useUsersList with tenant scope" but the shared `User` type has no `tenantId`. For mock mode tenant scope is implicit (single tenant). Real tenant scoping arrives with §M4.4 (real auth cutover) when JWT will carry `tenantId` and the API endpoint will filter server-side.
- **Tenant + practice filters on Verifications** — built the HPCSA/SANC filter; tenant/practice filters are deferred to the same M4.4 moment because verification fixtures don't carry tenant/practice metadata. The filter chip is intentionally absent for now rather than a non-functional decoration.
- **Approve / request-info / reject actions** show success/info/error toasts but are not wired to real mutations. The verifications controller exists on the API side, but mutation-hook wiring belongs to M2.2 when the queue is reused there — UI mutation surface should live next to the screen that owns the flow.
- **System health on real API** — `/v1/health` endpoint shape may differ from the new `SystemHealth` type. Verify when M4.4 lands; type may need a tweak or a server-side projection.
- **Feature flags + tenant summaries** — no API endpoint exists yet (`/v1/feature-flags`, `/v1/tenants`). Mobile is fixture-only until those endpoints ship — see [DEV.md](DEV.md) for backend scheduling.
- **`createResourceHooks` mock-toggle mismatch** — left in place for any future real-API-only callers, but everything in M3 routes via adapter-aware wrapper hooks. Worth a refactor in a later phase so `useList`/`useGet` from the factory honour `useApi()` and a per-resource `mock` accessor.

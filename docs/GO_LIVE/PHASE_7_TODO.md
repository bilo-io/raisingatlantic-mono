# Phase 7 — Observability & Monitoring: Outstanding Items

> Auto-generated by `/golive-phase` on 2026-06-03 from `docs/GO_LIVE/DEV.md`.
> Update manually as tasks complete.

## Completed this session

**§7.1 Logging**
- [x] Structured JSON logging on NestJS API (`nestjs-pino`) wired in `app.module.ts` via `buildLoggerConfig()`. Cloud Logging transport (`@google-cloud/logging-pino`) activates when `LOG_TRANSPORT=gcp`.
- [x] Request correlation IDs — `CorrelationIdMiddleware` reads/sets `X-Request-Id`, propagates via `nestjs-cls` AsyncLocalStorage; Pino's `genReqId` echoes the id on the response.
- [x] PII redaction — central path list in `src/apps/api/src/common/logging/redact-paths.ts`; pino `redact` config in `logger.config.ts`; spec at `common/test/redact-paths.spec.ts`.
- [x] Log retention — new `infra/modules/log-sink` provisions GCS archive with 30d STANDARD → COLDLINE → 395d delete lifecycle. Wired per-env in `infra/envs/{dev,staging,prod}/monitoring.tf` behind `var.enable_log_sink`.

**§7.2 Metrics & Tracing**
- [x] OpenTelemetry SDK — `src/apps/api/src/instrumentation.ts` initialises `NodeSDK` with auto-instrumentations, Cloud Trace + Cloud Monitoring exporters. Loaded as the first import of `main.ts`. Gated by `OTEL_ENABLED=true` so unit tests don't load it.
- [x] `core/telemetry/gcp/*.service.ts` rewritten to delegate cleanly: `GcpLoggerService` → pino sink, `GcpTracingService` → OTel tracer, `GcpMetricService` → OTel meter (counters + histograms cached), `GcpErrorReportingService` → Sentry capture.
- [x] Cloud Monitoring dashboards — `infra/envs/prod/monitoring.tf` defines `api_latency`, `api_errors`, `business_metrics` dashboards (gated by `var.enable_dashboards`).

**§7.3 Error Tracking**
- [x] Sentry SDKs landed: API (`@sentry/nestjs` in `instrumentation.ts` + `SentryModule.forRoot` + `SentryGlobalFilter`), Web (`@sentry/nextjs` with client/server/edge configs + `withSentryConfig` wrapper + `instrumentation.ts` hook), Mobile (`@sentry/react-native` initialised from `lib/sentry.ts`, wrapped via `Sentry.wrap(RootLayout)`).
- [x] Source-map upload in CI — `.github/workflows/cd-app.yml` `sentry-release` job tags the release with `${{ github.sha }}` and uploads API source maps via `sentry-cli`. Web maps auto-upload via `withSentryConfig` when `SENTRY_AUTH_TOKEN` is set during `next build`.
- [x] Release tagging tied to Git SHA — both API and Web releases use `${{ github.sha }}` (reusing the existing image-tag convention from Phase 9.1).
- [x] Slack `#alerts-prod` + email digest notification channels — `google_monitoring_notification_channel.slack_alerts_prod` + `email_digest` in `infra/envs/prod/monitoring.tf`, with `lifecycle.ignore_changes` on Slack auth label so token rotation doesn't fight Terraform.
- [x] Sentry IaC — `sentry_project` resources for api / web / mobile across dev / staging / prod (gated by `var.enable_sentry_projects`).

**§7.4 Uptime & SLOs**
- [x] Cloud Monitoring uptime checks (`google_monitoring_uptime_check_config` for `/v1/health` and `/`) — 60s interval in prod/staging, 300s in dev. Gated by `var.enable_uptime_checks`.
- [x] BetterStack monitors mirror the same endpoints (independent second source) — gated by `var.enable_betterstack`.
- [x] Alert policies — `alert_error_rate`, `alert_p95_latency`, `alert_db_connections`, `alert_uptime_api` instantiated via `infra/modules/monitoring-alert`. Each carries a `documentation_content` link to `docs/runbooks/on-call.md`.
- [x] On-call runbook — `docs/runbooks/on-call.md`. Documents single-on-call interim posture and the alert → Slack → PagerDuty path.
- [x] SLO ADR — `docs/adr/0004-slos.md` marked **DRAFT** with the DEV.md default targets (99.5% / p95 < 500ms) and the error-budget policy.

## Still outstanding

- [ ] **§7.4 Define SLOs** — `[ ]` retained pending `PRODUCT` sign-off on the DRAFT ADR (`docs/adr/0004-slos.md`). Per the planning conversation, the checkbox flips once PRODUCT explicitly accepts the targets.
- [/] **§7.4 Public status page** — Terraform `betterstack_status_page` authored; awaits (a) live BetterStack account, (b) `status.raisingatlantic.com` Cloud DNS CNAME — depends on Phase 1.3.
- [/] **§7.4 On-call rotation** — runbook + Slack channel routing landed; PagerDuty escalation policy + schedule deferred until a second on-call exists.
- [/] **§7.2 Custom business metrics** — `ra_signups_total` (users) and `ra_verifications_pending` (verifications) are wired; `ra_vaccinations_due` depends on an EPI scheduler that doesn't yet exist as a service. Flag added to PHASE_7_TODO.md to land alongside the EPI scheduler.

## Blockers / notes

- **Phase 1.2 GCP bootstrap is 0%.** No live `ra-prod` project, no Cloud Logging API enabled, no Workload Identity Federation for the monitoring SAs. Every `var.enable_*` flag in `infra/envs/*/variables.tf` defaults to `false`. Flip them in `terraform.tfvars` once Phase 1.2 completes:
  - `enable_log_sink`
  - `enable_uptime_checks`
  - `enable_alert_policies`
  - `enable_dashboards`
- **Live Sentry organisation** required to flip `enable_sentry_projects`. Sentry projects are authored under `raising-atlantic` org slug (current default).
- **Live BetterStack account** required to flip `enable_betterstack`. Same for `betterstack_status_page`.
- **Slack workspace + `#alerts-prod` webhook URL** (Phase 11.1) required to populate `slack_alerts_webhook_url` and flip `enable_alert_policies` for critical-severity routing.
- **Global TelemetryModule refactor** — services currently register `GcpLoggerService`/`GcpTracingService`/etc. providers per-module (e.g. `tenants.module.ts`, `users.module.ts`). A future cleanup PR can consolidate into a single global module; not load-bearing for the Phase 7 checklist.
- **EAS Build Sentry hook** — `app.json` includes the `@sentry/react-native/expo` plugin; once EAS Build is run end-to-end (Phase 10), confirm source maps actually upload. Add a smoke test in PHASE_10_TODO.md.

## Verification before shipping

- `moon run api:lint && moon run api:test && moon run api:typecheck && moon run api:build`
- `moon run web:lint && moon run web:build`
- `moon run mobile:lint && moon run mobile:typecheck`
- `cd infra && terraform fmt -recursive` clean.
- `terraform -chdir=infra/envs/prod init -backend=false && terraform -chdir=infra/envs/prod validate` clean (and same for dev/staging).
- Local API boot emits JSON logs with `reqId`; `redact-paths.spec.ts` passes.

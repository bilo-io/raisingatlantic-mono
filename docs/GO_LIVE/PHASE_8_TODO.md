# Phase 8 — Email, SMS & Notifications: Outstanding Items

> Auto-generated by `/golive-phase` on 2026-06-02. Update manually as tasks are completed.

## Completed this session

- **Framework-agnostic notification wrapper landed** at `src/core/notifications/`:
  - `IEmailService`, `ISmsService`, `IPushNotificationService`, `INotificationDispatcher` interfaces.
  - No-op adapters (default).
  - `NodemailerEmailService` (SMTP, replaces the old `MailService`).
  - `DefaultNotificationDispatcher` composing the three transports.
  - `NotificationsModule` (`@Global()`) wired into `AppModule` — any feature module can `@Inject(NOTIFICATION_TOKENS.Dispatcher)`.
- Dispatcher injected into `LeadsService` (live), `UsersService.create/update`, `AppointmentsService.create/update`, `ChildrenService.create` with `TODO(phase-8)` seams marking exactly where the real sends will live.
- `VerificationsService` carries a top-of-file TODO for the eventual approve/reject mutation endpoints.
- `.env.example` grouped under `# Notifications (Phase 8)` with placeholders for every candidate provider in the go-live doc.
- Mobile `AuthContext` carries a TODO for Expo push token capture after sign-in (DEV.md §8.3).
- Wrapper test coverage: 4 spec files (`noop-email`, `noop-sms`, `noop-push`, `default-dispatcher`).

The wrapper does **not** complete any §8 checkbox — every item below requires a chosen provider.

## Still outstanding

### §8.1 Transactional email
- [ ] Choose provider (SendGrid preferred, Postmark / AWS SES alternates)
- [ ] Configure custom sending domain (`mail.raisingatlantic.com`) with SPF + DKIM + DMARC
- [ ] Branded HTML templates (welcome, verification, password reset, EPI reminder, billing receipt, lead notification)
- [ ] Bounce + complaint handling (auto-suppress invalid addresses)
- [ ] One-click unsubscribe headers for marketing email
- [ ] Implement the chosen provider adapter under `src/core/notifications/<provider>/` and swap `NotificationsModule.useFactory` to construct it

### §8.2 SMS / WhatsApp
- [ ] Choose provider (Twilio / Clickatell / Infobip)
- [ ] WhatsApp Business API template approval
- [ ] Opt-in capture before sending any SMS / WhatsApp message
- [ ] Stop-word handling (STOP / OPT-OUT / UITSKAKEL)
- [ ] Implement adapter and bind in `NotificationsModule`

### §8.3 Push Notifications (mobile)
- [ ] Install and configure `expo-notifications` in the mobile app
- [ ] Wire token capture in `AuthContext.signInAs` (TODO already present)
- [ ] `POST /v1/users/me/push-tokens` API endpoint (stores token on `User` entity)
- [ ] Notification preferences UI (which categories the user wants)
- [ ] Quiet hours enforcement in `DefaultNotificationDispatcher.notifyUser` (currently a no-op)
- [ ] Implement Expo adapter and bind in `NotificationsModule`

## Blockers / notes

- Provider selection is a product/ops call, not a dev decision. The wrapper is in place specifically so the swap is a single file (`NotificationsModule.useFactory`) plus a new adapter class.
- `INotificationDispatcher.notifyUser` is intentionally stubbed — it needs the user-preference table from §8.3 before it can fan out. Call sites currently invoke the transport methods directly (`.email()`, `.sms()`, `.push()`) when they're ready.
- All TODOs are findable via `grep -rn "TODO(phase-8)" src/`.

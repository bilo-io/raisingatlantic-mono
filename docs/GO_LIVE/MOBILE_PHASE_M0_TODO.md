# Phase M0 — Foundations: Outstanding Items

> Auto-generated by `/golive-phase` on 2026-06-02 from `docs/GO_LIVE/MOBILE.md`. Update manually as tasks are completed.

## Completed this session

- §M0.1 — `createResourceHooks` factory ported to `src/apps/mobile/lib/api/createResourceHooks.ts` (web's `toast-bridge` swapped for a sonner-native wrapper).
- §M0.1 — Six domain adapters under `src/apps/mobile/lib/api/adapters/` (`child`, `practice`, `verification`, `appointment`, `record`, `user`), each switching on `useApi()`.
- §M0.1 — Six domain hook files under `src/apps/mobile/lib/api/hooks/` plus a barrel.
- §M0.1 — All types imported from `@raising-atlantic/types`; no inline redefinition.
- §M0.2 — `data-source.ts` ported, gated by `EXPO_PUBLIC_USE_API`. Default `"false"` in `app.json` `expo.extra`. Documented in `src/apps/mobile/README.md`.
- §M0.3 — Seven fixture seed modules (`children`, `epi-schedule`, `growth`, `milestones`, `patients`, `verifications`, `practices`) plus two supporting fixtures (`users`, `appointments`).
- §M0.3 — SA DoH 2024/2025 EPI schedule promoted to `src/pkgs/types/src/clinical.ts` (`standardVaccinationSchedule`). Mobile fixture re-exports.
- §M0.3 — No real PII in any fixture (synthetic SA names; invented IDs).
- §M0.4 — `signFixtureToken()` builds an unsigned dev JWT with `{ sub, role, tenantId, practiceIds, iat, exp }`. Throws if `!__DEV__`.
- §M0.4 — `auth-header.ts` extended with `setAuthToken()` + `Authorization: Bearer …` header emission alongside the legacy `X-User-Id` / `X-User-Role` fallback.
- §M0.4 — `AuthContext.signInAs()` and hydration both publish the fixture token in `__DEV__` only; `signOut()` clears it.
- §M0.5 — `<QueryClientProvider>` confirmed wrapping the app at `app/_layout.tsx`. `staleTime: 30s` / `gcTime: 5m` confirmed in `query-client.ts`.

## Still outstanding

- **§M0.4 — API dev environment must accept the fixture token.** The current API guard (`src/apps/api/src/common/guards/jwt-auth.guard.ts`) returns `true` without validating the signature, so the unsigned fixture token is accepted today. As soon as DEV.md §2 lands real verification, the dev environment will need to either trust the unsigned `alg: none` header or accept a configurable dev signing key. Coordinate before the API auth work merges.
- **§M0.5 — React Query devtools / Reactotron wiring.** Neither tool ships first-party Expo/Hermes support; both require a dev-client native module change. Deferred to a small follow-up so the M0 PR stays focused on the data layer. Consider the Expo dev-plugin `@dev-plugins/react-query` once it stabilises for SDK 54+.

## Blockers / notes

- The fixture JWT uses `alg: none` (header `{"alg":"none","typ":"JWT"}`) and has an empty signature segment. This is intentional — there is no production-bound secret in the bundle — and is gated behind `__DEV__`. When DEV.md §2 introduces real JWT verification, the fixture signer is **replaced wholesale** at §M4.4 (Real auth cutover), not extended.
- `src/apps/web/src/data/vaccinations.ts` was **not** retargeted to consume from `@raising-atlantic/types` in this PR — that change is a follow-up sweep so M0's surface area stays mobile-only.
- Hooks for `users.useDelete` and similar admin-write mutations are exposed but the API endpoints don't all exist yet — they will return 404 in real-API mode until DEV.md backend work catches up. Fixture mode is unaffected.
- `patch-package` postinstall is wired but no patches are present in the worktree — the existing repo behaviour is untouched.

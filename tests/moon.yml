project:
  name: tests
  
tasks:
  # install:
  #   deps:
      # - 'npm install'

  # build:
  #   deps:
  #     - 'api:build'
  #     - 'web:build'
  #     - 'ui:build'

  # dev:
  #   deps:
  #     - 'api:dev'
  #     - 'web:dev'
  #   options:
  #     runInCI: false

  test:
    command: 'npm run test:postman:local'

  clean:
    command: 'rm -rf node_modules'
